-- ==================== AUTO COLLECT BULL'S ESSENCE ====================
local AutoCollectEnabled = false
local CurrentTarget = nil

local function FindNearestBullEssence()
    local character = game.Players.LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return nil end
    
    local rootPos = character.HumanoidRootPart.Position
    local nearest = nil
    local shortestDist = math.huge

    for _, item in ipairs(workspace.Items:GetChildren()) do
        if item.Name == "Bull's essence" then
            local part = item:FindFirstChild("HumanoidRootPart") or item:FindFirstChildWhichIsA("BasePart")
            if part then
                local dist = (part.Position - rootPos).Magnitude
                if dist < shortestDist then
                    shortestDist = dist
                    nearest = item
                end
            end
        end
    end
    return nearest
end

local function AutoCollectLoop()
    while AutoCollectEnabled and game.Players.LocalPlayer.Character do
        local character = game.Players.LocalPlayer.Character
        local root = character:FindFirstChild("HumanoidRootPart")
        if not root then break end

        -- Cek apakah target saat ini masih ada
        if CurrentTarget and CurrentTarget.Parent then
            wait(0.5)  -- cek setiap 0.5 detik
            continue
        end

        -- Cari target baru
        local target = FindNearestBullEssence()
        if target then
            CurrentTarget = target
            local part = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChildWhichIsA("BasePart")
            
            if part then
                root.CFrame = part.CFrame + Vector3.new(0, 6, 0)  -- TP sekali ke atas item
                print("Teleported to:", target:GetFullName())
            end
        else
            wait(1)
        end

        wait(0.3)  -- anti lag
    end
end

-- ==================== TOGGLE ====================
ItemsTab:Toggle({
    Title = "Auto collect",
    Desc = "Collect priority items without waiting for the round countdown",
    Callback = function(state)
        AutoCollectEnabled = state
        CurrentTarget = nil
        
        if state then
            WindUI:Notify({Title = "Auto Collect", Content = "ON - Targeting nearest Bull's Essence"})
            task.spawn(AutoCollectLoop)  -- Jalankan di thread terpisah
        else
            WindUI:Notify({Title = "Auto Collect", Content = "OFF"})
        end
    end
})

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = ".ftgs hub  |  WindUI Example",
    Folder = "ftgshub",
    Icon = "solar:folder-2-bold-duotone",
    NewElements = true,

    OpenButton = {
        Title = "Open .ftgs hub UI",
        Enabled = true,
        Draggable = true,
    },
    Topbar = {
        Height = 44,
        ButtonsType = "Mac",
    },
})

-- Main
local CombatTab = Window:Tab({
    Title = "Main",
    Icon = "solar:settings-bold",
})

CombatTab:Toggle({
    Title = "Kill Aura",
})
CombatTab:Space()

CombatTab:Toggle({
    Title = "Auto Parry",
})
CombatTab:Space()

CombatTab:Toggle({
    Title = "Auto Dodge",
})

-- Items
local ItemsTab = Window:Tab({
    Title = "Items",
    Icon = "solar:settings-bold",
})

ItemsTab:Toggle({
    Title = "Auto collect",
    Desc = "Collect priority items without waiting for the round countdown",
    Callback = function(state)
        if state then
            StartAutoCollect()
            WindUI:Notify({Title = "Auto Collect", Content = "ON - Targeting Bull's Essence"})
        else
            StopAutoCollect()
            WindUI:Notify({Title = "Auto Collect", Content = "OFF"})
        end
    end
})

ItemsTab:Space()

ItemsTab:Toggle({
    Title = "Auto pick up",
    Desc = "",
})

ItemsTab:Space()

ItemsTab:Toggle({
    Title = "Auto heal",
    Desc = "Uses healing items when HP drops",
})

ItemsTab:Space()

ItemsTab:Toggle({
    Title = "Auto use Permanent items",
    Desc = "Uses parmenent boots on pickup",
})


-- Teleports
local MiscTab = Window:Tab({
    Title = "Teleports",
    Icon = "solar:settings-bold",
})

MiscTab:Toggle({
    Title = "Speed Hack",
})

MiscTab:Space()

MiscTab:Slider({
    Title = "WalkSpeed",
    Value = { Min = 16, Max = 200, Default = 60 },
})

-- Coombat
local MiscTab = Window:Tab({
    Title = "Teleports",
    Icon = "solar:settings-bold",
})

-- Beta
local BetaTab = Window:Tab({
    Title = "Beta",
    Icon = "solar:settings-bold",
})

BetaTab:Toggle({
    Title = "Collect Crates",
})

BetaTab:Space()

BetaTab:Toggle({
    Title = "Auto Collect crates",
})

BetaTab:Space()

BetaTab:Toggle({
    Title = "Crate Aura",
})

BetaTab:Space()

BetaTab:Toggle({
    Title = "Early auto collect",
})

BetaTab:Space()

-- Safety
local MiscTab = Window:Tab({
    Title = "Safety",
    Icon = "solar:settings-bold",
})

-- Settings
local MiscTab = Window:Tab({
    Title = "Settings",
    Icon = "solar:settings-bold",
})

WindUI:Notify({
    Title = "Combat Hub",
    Content = "Loaded successfully",
})
